- On Connect 
	$ date; date -u
	$ whoami &&  id && w
	$ ifconfig
	$ uname -a
	$ cat /etc/*eleas*
	$ hostname
	$ netstat -auntp
	$ ps -elfH 
	$ ls -latr /
	$ ls -latr /tmp
	$ ls -latr .
	$ ls -latr ..
	$ sudo ls -latr /root
	$ sudo ls -latr /var/spool/cron	#tail -n 1000 per file 
	$ df
	$ sudo ls -latr /var/*acc*
	$ sudo ls -latr /var/*log*/
	$ sudo ls -latr /var/log/*
	$ sudo cat /var/log/* | grep [logged in user account]
	$ sudo ls -la /etc/*syslog* read all the config files
	$ sudo cat /etc/audit/auditd.conf
	$ sudo cat /etc/audit/rules.d/audit.rules
	$ for user in $(cut -f1 -d: /etc/passwd); do echo "###### $user crontab is:";sudo cat /var/spool/cron/{crontabs/$user,$user} 2>/dev/null; done	#:
	$ cat /etc/crontab
	$ ls -la /etc/cron.* # As needed, examine the files/scripts shown in the directory listing of the cron.* directories
	$ sestatus OR getenforce
	$ sudo cat /root/.bash_history
	$ cat ~/.bash_history
	$ date
	$ find / \( -path /proc -prune -o -path /sys -prune \) -o -mmin -<duration since initial connection> -type f -print0 | xargs -0 ls -latr #NOTE there is a "-" a.k.a minus symbol preceding the "duration since initial connection"
- Before Disco
	- REMOVE ARTIFACTS
		$ ls -latr /tmp
		$ ps -elfH
		$ netstat -lptn
		$ netstat -natup
		$ ls -latr /var/log
		$ w
		$ date
		$ find / \( -path /proc -prune -o -path /sys -prune \) -o -mmin -120 -type f -print0 | xargs -0 ls -latr	

